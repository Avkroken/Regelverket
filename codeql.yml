name: "Avkroken organization CodeQL"

# Central baseline for GitHub CodeQL default setup.
# GitHub merges this file with the configuration generated by default setup.
# Keep the default query suite until canary results confirm merge-protection thresholds.
